pipeline {
  agent any

  parameters {
    booleanParam(name: 'Bridge',        defaultValue: true,  description: 'Run Bridge stage')
    booleanParam(name: 'DMA',           defaultValue: true,  description: 'Run DMA stage')
    booleanParam(name: 'FATFS',         defaultValue: true,  description: 'Run FATFS stage')
    booleanParam(name: 'FPGA_Manager',  defaultValue: true,  description: 'Run FPGA Manager stage')
    booleanParam(name: 'GPIO',          defaultValue: true,  description: 'Run GPIO stage')
    booleanParam(name: 'IOSSM',         defaultValue: true,  description: 'Run IOSSM stage')
    booleanParam(name: 'QSPI',          defaultValue: true,  description: 'Run QSPI stage')
    booleanParam(name: 'REBOOT',        defaultValue: true,  description: 'Run Reboot stage')
    booleanParam(name: 'SDM_Mailbox',   defaultValue: true,  description: 'Run SDM Mailbox stage')
    booleanParam(name: 'SDMMC',         defaultValue: true,  description: 'Run SDMMC stage')
    booleanParam(name: 'Timer',         defaultValue: true,  description: 'Run Timer stage')
    booleanParam(name: 'USB',           defaultValue: true,  description: 'Run USB stage')
    booleanParam(name: 'WDT',           defaultValue: true,  description: 'Run WDT stage')
  }

  stages {
    stage('Bridge') {
      when { expression { params.Bridge } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/Bridge test',
              wait: true,
              propagate: false
            )
            echo "🔷 Bridge #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p Bridge'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/Bridge test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt BRIDGE/bridge_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('DMA') {
      when { expression { params.DMA } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/DMA test',
              wait: true,
              propagate: false
            )
            echo "🔷 DMA #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p DMA'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/DMA test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt DMA/dma_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('FATFS') {
      when { expression { params.FATFS } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/FATFS Test',
              wait: true,
              propagate: false
            )
            echo "🔷 FATFS #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p FATFS'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/FATFS test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt FATFS/fatfs_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('MAILBOX') {
      when { expression { params.SDM_Mailbox } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/SDM Mailbox Test',
              wait: true,
              propagate: false
            )
            echo "🔷 MAILBOX #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p MAILBOX'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/SDM Mailbox Test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt MAILBOX/mailbox_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('QSPI') {
      when { expression { params.QSPI } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/QSPI Test',
              wait: true,
              propagate: false
            )
            echo "🔷 QSPI #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p QSPI'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/QSPI Test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt QSPI/qspi_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('REBOOT') {
      when { expression { params.REBOOT } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/Reboot Test',
              wait: true,
              propagate: false
            )
            echo "🔷 REBOOT #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p REBOOT'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/Reboot Test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt REBOOT/reboot_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('SDMMC') {
      when { expression { params.SDMMC } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/SDMMC Test',
              wait: true,
              propagate: false
            )
            echo "🔷 SDMMC #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p SDMMC'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/SDMMC Test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt SDMMC/sdmmc_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('Timer') {
      when { expression { params.Timer } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/Timer test',
              wait: true,
              propagate: false
            )
            echo "🔷 Timer #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p Timer'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/Timer test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt Timer/timer_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('USB3') {
      when { expression { params.USB } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/USB Test',
              wait: true,
              propagate: false
            )
            echo "🔷 USB3 #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p USB'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/USB Test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt USB/usb_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('WDT') {
      when { expression { params.WDT } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/WDT Test',
              wait: true,
              propagate: false
            )
            echo "🔷 WDT #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p WDT'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/WDT Test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt WDT/wdt_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('IOSSM') {
      when { expression { params.IOSSM } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/IOSSM Test', 
              wait: true, 
              propagate: false
            )
            echo "🔷 IOSSM #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p IOSSM'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/IOSSM Test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt IOSSM/iossm_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('GPIO') {
      when { expression { params.GPIO } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/GPIO Test',
              wait: true,
              propagate: false
            )
            echo "🔷 GPIO #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p GPIO'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/GPIO test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt GPIO/gpio_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

    stage('FPGA Manager') {
      when { expression { params.FPGA_Manager } }
      steps {
        catchError(buildResult: 'SUCCESS', stageResult: 'FAILURE') {
          script {
            def result = build(
              job: 'CI-CD Pipelines/CLI Tests/FPGA Manager Test',
              wait: true,
              propagate: false
            )
            echo "🔷 FPGA Manager #${result.number} → ${result.result}"

            dir('artifacts') {
              sh 'mkdir -p FPGA_Manager'
              copyArtifacts(
                projectName: 'CI-CD Pipelines/CLI Tests/FPGA Manager Test',
                selector: specific("${result.number}"),
                filter: 'test_results.txt',
                optional: true
              )
              sh """
                if [ -f test_results.txt ]; then
                  mv test_results.txt FPGA_Manager/fpga_manager_test_results.txt
                fi
              """
            }
          }
        }
      }
    }

  }

  post {
    always {
      echo "🏁 All stages complete"
      archiveArtifacts artifacts: 'artifacts/**/*', allowEmptyArchive: true
    }
  }
}
